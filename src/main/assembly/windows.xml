<?xml version="1.0"?>
<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.0.0 http://maven.apache.org/xsd/assembly-2.0.0.xsd">

    <id>windows</id>

    <formats>
        <format>zip</format>
    </formats>

    <dependencySets>
        <dependencySet>
            <useProjectArtifact>true</useProjectArtifact>
            <outputDirectory>lib</outputDirectory>
            <excludes>
                <exclude>org.springframework.boot:spring-boot-devtools</exclude>
            </excludes>
        </dependencySet>
    </dependencySets>

    <fileSets>
        <fileSet>
            <!-- Workaround to create logs directory -->
            <directory>${dist.dir}</directory>
            <outputDirectory>logs</outputDirectory>
            <excludes>
                <exclude>*/**</exclude>
            </excludes>
        </fileSet>
        <fileSet>
            <directory>${dist.jdk.dir}</directory>
            <outputDirectory>jdk</outputDirectory>
            <excludes>
                <exclude>demo/**</exclude>
                <exclude>sample/**</exclude>
                <exclude>src.zip</exclude>
            </excludes>
        </fileSet>
        <fileSet>
            <directory>${dist.web.dir}</directory>
            <outputDirectory>web/public</outputDirectory>
        </fileSet>
    </fileSets>

    <files>
        <file>
            <source>${dist.dir}/service.exe</source>
            <outputDirectory/>
            <destName>${dist.project.id}.exe</destName>
        </file>
        <file>
            <source>${dist.dir}/service.xml</source>
            <outputDirectory/>
            <destName>${dist.project.id}.xml</destName>
        </file>
        <file>
            <source>${dist.dir}/service.exe.config</source>
            <outputDirectory/>
            <destName>${dist.project.id}.exe.config</destName>
        </file>
    </files>
</assembly>
